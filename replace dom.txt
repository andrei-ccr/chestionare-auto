DELIMITER ^^
CREATE PROCEDURE schimba()
BEGIN
    DECLARE counter INT DEFAULT 0;

    WHILE counter < 10 DO
        SET @ss = (SELECT `variante` FROM `intrebari` WHERE id=counter);
        SELECT @ss;
        SET counter = counter + 1;
    END WHILE;
END;
DELIMITER ;